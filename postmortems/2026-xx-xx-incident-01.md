## Action Items (Prevent recurrence / harden)
- [x] Add healthcheck to docker-compose (checks `/health`)
- [x] Add restart policy (`restart: unless-stopped`)
- [ ] Continue improving runbook (add more triage steps and links)
